# =====================================================================
# FRONTEND GIT IGNORE CONFIGURATION (REACT / VITE)
# =====================================================================
# Prevents node_modules, build outputs, environment credentials, and
# local web caches from being committed to Git repository.

# ---------------------------------------------------------------------
# 1. Environment Credentials (CRITICAL - DO NOT COMMIT)
# ---------------------------------------------------------------------
.env
.env.*
!.env.example
.env.local
!pyproject.toml

# ---------------------------------------------------------------------
# 2. Node Dependencies
# ---------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------------------------------------------------------------------
# 3. Production Build Outputs
# ---------------------------------------------------------------------
dist/
build/
.out/

# ---------------------------------------------------------------------
# 4. OS & Editor Temporary Files
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
