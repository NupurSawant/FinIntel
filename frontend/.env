# Production Backend URL (Used when deployed live on Vercel / Render)
# Note: When running on localhost (localhost:3000), frontend automatically connects to http://localhost:8000
VITE_API_BASE_URL=https://financeintel.onrender.com
